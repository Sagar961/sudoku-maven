apiVersion: v1
kind: Service

metadata:
  name: sudoku-service

spec:
  type: NodePort

  selector:
    app: sudoku

  ports:
    - protocol: TCP
      port: 8081
      targetPort: 8081
      nodePort: 30080
